# Asynchronous-TDOA-with-Reference-Aerial-Synchronization
The method for determining the coordinates of aerial objects using the difference-range method, which uses an independent aerial object with known coordinates to synchronize time scales between reception areas.
